# Claude Skills

Drop skill folders here. This directory is linked to `~/.claude/skills` by the
`agents` home-manager module (recursively, so Claude Code can still create its
own skills alongside the managed ones).
