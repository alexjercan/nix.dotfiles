---
name: extra
description: Fixture skill for the agents module checks.
---

# Extra

This directory exists so the checks can deploy a skill and assert where it
lands. Its sibling directory holds no SKILL.md, which is the invalid case.
