# Restrict task statuses to tatr values

- STATUS: CLOSED
- PRIORITY: 100
- TAGS: agents, tatr

## Goal

Persist the rule that task status values are limited to `OPEN`, `IN_PROGRESS`, and `CLOSED`.

## Completion

- Added the rule to the generated global `AGENTS.md` source.
- Scufris repository guidance now requires the tatr skill and CLI for task records.
