# Rule inventory before compaction

Each row lists behavior that must remain in the core, move to a reachable
reference/tool, or be explicitly retired.

| File | Required rules |
| --- | --- |
| `flow/SKILL.md` | One task by default; phase context only; request/code outrank task text; ask on ambiguous WHAT and record decisions; user-approved plan gate; lessons -> work/review loop -> compound -> land; new work gets a task; new lessons re-audit queued work; default-branch checks, proofs, lessons, ledger gate; stop cases; terminal output; conditional epic/landing/resume loads. |
| `flow/epic.md` | Containers only for explicit multi-feature goals; EPIC/STORY relationships and cross-repo limits; exemptions; compact index owns destination/decisions/fog/scope while frontier is derived; required sections; choose top READY without reading all children; atomic shared claims; verified close; split oversized/inseparable children with user. |
| `plan/SKILL.md` | Read request/context/code/backlog; spike unknown WHAT; expose mutually exclusive constraint; one cohesive task; one `tatr new` per command; priority vs dependency; update named task; concrete ordered file-naming Steps and proof-bearing DoD; Notes evidence/assumptions/questions; verify load-bearing claims; conditional steps are decisions; record architecture choices; present hard gate; concise plans; audit new state routes; plan from code; output and conditional references. Retired: the old core said a base-red `cmd:` proof was broken. That contradicted `proofs.md` and test-first sabotage, where the final-state proof must be red while the change is absent. The compact core follows the proof reference. |
| `plan/lanes.md` | One planner default; lanes only for expensive forks, independent domains, oversized Epics, or user request; 2-3 distinct whole-task lenses; identical isolated read-only packets; 400-word plans; orchestrator verifies and synthesizes one plan/decision; discard candidate scratch; fact disputes get checked; no writes/worktrees. |
| `plan/proofs.md` | Every DoD item has parenthesized test/cmd/manual proof; backticks for tests/commands; unprovable criteria move to Notes; manual is genuine user judgement and does not block land; tests drive work; absence grep excludes task history and prose with syntax-specific limits/cross-check; proofs must fail on their own boundary and preserve exit codes; sabotage each proof at plan time, one clause at a time, and reject base-green proofs. |
| `work/SKILL.md` | Select task and context; sprout from intended base then WORKING gate; ask on unrelated dirty main; branch fallback; absolute worktree paths; reconcile plan with code; test/cmd proof red before implementation; integration altitude preferred; never self-confirm manual; sync docs and decisions; run full checks/proofs; close-out plus code/task commit then REVIEWING; one task/worktree; tick literal full step; no weakened tests; protect shared checkout; complete grep sweeps; production test rigs; split oversized work; bounded output and conditional references. |
| `work/verify.md` | Run canonical suite, each proof independently, task/ledger checks; manual stays pending; proofs must fail; preserve check exit codes; cover shared systems/all targets/fixture pins; commit before sabotage and restore from HEAD; sweep live docs but not task history; search symbols and concepts before removal; merge default into feature, then fully reverify and prove ancestry. |
| `review/SKILL.md` | Review branch diff against task; substantive round 1 is out of context; fresh prompt excludes implementation assumptions; primary reruns checks and re-derives a claim; inspect all dimensions; findings require severity/location/action; REQUEST_CHANGES for open BLOCKER/MAJOR else APPROVE; list manual items; verify responses and only new regressions in later rounds; user arbitrates after three; actionable diff-scoped severity; commit every round; bounded output and conditional dimensions/rounds/lanes. |
| `review/lanes.md` | One reviewer default; lanes only for costly/quiet broad risks or user request; open applicable behavior/proofs, correctness/security/concurrency, and design/docs lanes; same isolated read-only packet; 400-word returns, no edits/verdict; primary runs skipped proofs, verifies/deduplicates/ranks, writes one round/verdict; read-only shared worktree; mutating checks serialized by primary. |
| `spike/SKILL.md` | Frame answerable time-boxed question; choose repository research, prototype, or mixed mode after reading code; compare real alternatives; recommend by effort/risk/fit/reversibility and retain unknowns; create/scaffold self-contained cited SPIKE with valid status; settled choice gets DECISION; seed coarse tasks one command at a time without Steps; exploratory code is not production; multi-task result gets Fix record; DROPPED seeds none; close through review/retro; time-box, retain negative result, tunable weak preference; concise SPIKED output and conditional references. |
| `compound/SKILL.md` | Start only after APPROVE and clean task; read task/review/log; process-focused specific root-cause retro; scaffold RETRO; append/bump concise ledger lessons and keep counts bare; third occurrence moves pending with promotion order; record no disposition and create tasks for follow-up work; commit before landing; avoid record duplication; report slugs and advance only through tatr. |
| `lessons/SKILL.md` | Ledger and reference docs are only durable homes; run at finish/release/request; locate format and scratch; distill insight, migrate reference detail, discard transient notes; append/bump then ledger-check; ask user for every disposition and use `tatr ledger`; PROMOTE creates normal reviewed task; clear scratch only, never durable docs; no duplicate retro lessons; concise output and ledger reference. |
| `lessons/ledger.md` | Root ledger wins, duplicate flagged, otherwise create near durable docs; exact sections/count lifecycle; user owns PROMOTE/DEFER/RETIRE/ABSORBED through `tatr ledger`; PROMOTE stays pending until landed/applied marker; DEFER reopens after count rises; lint failures; promotion order tool > template > prose; no self-promotion; shrink prose when absorbed; retire then prune at release. |
| `sprout/SKILL.md` | CLI purpose/path; command contracts; new/show stdout composition and branch base; feature-name rules; project-scoped listing; destructive rm caveat; guarded atomic land; cold isolated caches; ignored-file cleanup; absolute paths; one task/worktree; land only from main as standalone call; sprout owns no task lifecycle; conditional reference. |
| `today/SKILL.md` | CLI owns journal read/write; agents never invoke bare editor mode; den resolution and entry naming; commands for path/create/show/tasks/habits/weight/macros/notes/day/den/help; JSON state fields and 1-based indices; most commands support JSON and diagnostics use stderr; path is read-only while others create; reread after removal and mutate once per call; day offsets; task/note/value/creation semantics; exit codes; parse JSON, default today. |
| `check.sh` | Enforce exact 300 router, 400 phase, 600 reference inclusive budgets without weakening other structural checks. |
| `README.md` | Document the exact enforced limits and keep budget semantics aligned with the checker. |
